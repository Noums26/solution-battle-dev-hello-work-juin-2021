// Le input est un tableau contenant un premier element qui represente l'egole necessaire au decollage et un dexieme qui represente le distance totale du voyage

const calcul = (input) => {
	return (parseInt(input[0]) + (parseInt(input[1])) * 5)
}